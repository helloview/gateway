services:
  nginx-gateway:
    image: nginx:1.27-alpine
    container_name: helloview-gateway
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
